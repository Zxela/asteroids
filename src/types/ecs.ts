/**
 * Core ECS Types
 *
 * Re-exports from the canonical ECS type definitions in src/ecs/types.ts.
 * This module exists for import convenience from the types barrel.
 */

export type { EntityId, Component, System, World, ComponentClass } from '../ecs/types'
